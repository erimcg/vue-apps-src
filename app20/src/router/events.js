import EventsView from '../views/events/EventsView.vue'

const routes = [
    {
        path: '/',
        component: EventsView
    },
    {
        path: '/events',
        component: EventsView
    }
]

export default routes
